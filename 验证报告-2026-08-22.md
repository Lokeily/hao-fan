# 好翻（open-translator-cn）v0.1.23 · 软件验证报告

> 验证时间：2026-08-22 · 验证方式：按合格程序员的软件验证流程，覆盖**可行性 / 安全性 / 稳定性**三大维度
> 被测版本：v0.1.23（本地，未发布） · 结论见文末

---

## 一、可行性验证（能不能构建、打包、跑起来）

| # | 验证项 | 方法 | 结果 |
|---|---|---|---|
| 1 | Chrome 平台构建 | `npm run build`（wxt → chrome-mv3） | ✅ 311.25 kB，含 postbuild 修正 popup/options/image-translate HTML |
| 2 | Firefox 平台构建 | `npm run build:firefox`（WXT 自动降级 MV2） | ✅ 311.32 kB，`background.scripts` 正确 |
| 3 | Chrome 商店包 | `npm run zip` → `open-translator-cn-0.1.23-chrome.zip` | ✅ 117.87 kB，`unzip -t` 无损坏，manifest/content.js 均在 |
| 4 | Firefox 商店包 | `npm run zip:firefox` → `...-firefox.zip` + `...-sources.zip` | ✅ 117.93 kB / 510.32 kB |
| 5 | Chrome manifest 审计 | 人工核对 | ✅ 权限最小（storage/activeTab/contextMenus/scripting）、CSP 严格（`script-src 'self'`）、host_permissions=`<all_urls>`（翻译任意网页所必需） |
| 6 | Firefox manifest 审计 | 人工核对 | ✅ MV2、稳定 gecko ID（`open-translator-cn@haofan`）、数据收集声明准确、CSP 严格 |
| 7 | 浏览器回归（加载编译产物） | Playwright + 真实 Chrome，32 用例 | ✅ 32/32（整页/两列/多列/网格/取消任务/自动翻译/无 Key 引导/深浅色/设置面板/稳定性循环/perf 基准 2001 段） |
| 8 | 真实 Chrome `--load-extension` 冒烟 | 脚本 `tests/browser/real-extension-smoke.mjs` | ⚠️ 本沙箱环境不支持加载扩展（无头/沙箱限制），**已备好脚本供本机桌面 Chrome 运行**；扩展代码本身已在 32 项浏览器回归中用编译产物验证 |

**可行性结论**：双平台可构建、可打包、可安装；编译产物在真实 Chrome 中的行为已由 32 项回归覆盖。唯一无法在本环境完成的是「带图形界面的真实加载 + 真实 API 翻译」冒烟（见文末待办）。

---

## 二、安全性验证

| # | 验证项 | 方法 | 结果 |
|---|---|---|---|
| 1 | 仓库无密钥残留 | 工作区 + git 全历史扫描 `sk-`/`ghp_`/`github_pat_`/AWS Key/私钥 | ✅ 工作区干净、历史 0 匹配，从未提交过密钥 |
| 2 | API Key 存储 | 仅 `storage.local`（`utils/storage.ts`），不随设置同步 | ✅ |
| 3 | API Key 传输 | 全部走 `Authorization` 头（LLM 用 `Bearer`、DeepL 用 `DeepL-Auth-Key`、微软用订阅 Key 头），**无任何 URL 携带**；发送前 `cleanSecret` 剥离空白/控制符/非 ASCII | ✅ 单测 ok 41/43/44 守护 |
| 4 | XSS | 全仓扫描：源码**零** `innerHTML=` / `insertAdjacentHTML` / `document.write` / `console.log`；译文一律 `textContent` + Shadow DOM 注入 | ✅ 无注入面 |
| 5 | Prompt Injection | 待译文本用 `<<<TRANSLATE_DATA>>>` 数据边界包裹；页面上下文降级为**数据块追加到 user 消息**（不进 system），含 `INJECTION_GUARD` 指令护栏；标题/前文截断剔除换行 | ✅ 单测 ok 32（页面标题注入指令时只出现在 user、不进 system）守护 |
| 6 | 权限最小化 | 4 项权限均有明确用例（storage=配置缓存统计、activeTab=按需注入、contextMenus=右键翻译、scripting=弹窗注入） | ✅ |
| 7 | 网络防护 | 超时 + AbortController、仅重试 429/5xx/timeout 且上限 1 次、SSE 流 finally 清理、MV3 断连取消 | ✅ 单测 ok 29（429 Retry-After）守护 |

**安全性结论**：无高危/中危问题。密钥全链路受控、无 XSS 注入面、Prompt Injection 双层防护、权限最小、历史无密钥。

---

## 三、稳定性验证

| # | 验证项 | 方法 | 结果 |
|---|---|---|---|
| 1 | 单元测试 | `npm run test` | ✅ **45/45**（批量协议/截断降级/坏 JSON 恢复/漏条目/429 重试/缓存/术语/句子缓存/流式/质量自检/注入防护/MT 引擎 ×3/恢复预算） |
| 2 | 类型检查 | `tsc --noEmit` | ✅ 0 错误 |
| 3 | 代码规范 | `eslint .` | ✅ 0 错误 0 警告 |
| 4 | 版本一致性 | `check:version` | ✅ 0.1.23 全文件一致 |
| 5 | 浏览器回归 | Playwright + 真实 Chrome | ✅ 32/32，含竞态专项（快速 翻译/取消/暂停/恢复 循环不卡死）与动态内容（DOM 变化后自动重译） |
| 6 | 性能基准 | `perf.spec.ts` 长文 2001 段 | ✅ 首译 90ms / 全管线 941ms / 无长任务卡顿（最长 205ms、>200ms 仅 1 次） |
| 7 | 竞态与并发 | 翻译取消 jobId 守卫、批/单并发上限（≤2 worker）、设置串行写队列、流式 15s 空闲超时 + 断连回退 | ✅ 逻辑逐行审过 + 专项用例覆盖 |
| 8 | 错误处理 | 网络失败/401/403/空译文/坏 JSON/超时均有明确中文提示与回退 | ✅ 单测覆盖 |

**稳定性结论**：45 单测 + 32 浏览器回归 + 性能基准全绿，竞态/并发/超时/恢复均有专项守护。

---

## 四、本次验证过程中修复/补强的点

1. **修复（Google 免 Key 引擎）**：`translateMT` 的端点 URL 原本硬编码，无法在测试中重定向。改为 `cfg.baseUrl || 默认`（与 DeepL/Microsoft 一致，生产行为不变），使 google 路径可被自动化测试。
2. **补测试（MT 引擎覆盖缺口）**：新增 3 个单测（Google 请求格式与响应解析 / DeepL Authorization 头 / Microsoft 订阅 Key 头），此前传统翻译引擎路径零覆盖；测试 mock 同步补记 `headers`。
3. **补真实浏览器冒烟脚本**：`tests/browser/real-extension-smoke.mjs`（需桌面 Chrome，本沙箱无法执行，留待本机验证）。

---

## 五、结论与待办

### 结论
✅ **可行性 / 安全性 / 稳定性三维度全部通过自动化与人工验证**：双平台构建打包有效、权限与密钥处理无风险、45 单测 + 32 浏览器回归 + 性能基准全绿、无高危问题。代码达到可发布水平。

### 待办（唯一未闭合项）
- **真实 API 端到端冒烟**：本沙箱无法访问外部翻译服务（Google 端点不可达）且无法加载图形化扩展。请在**你的本机桌面 Chrome** 执行一次：
  1. `npm run build` 后，Chrome `chrome://extensions` → 开发者模式 → 加载已解压的扩展（`.output/chrome-mv3`）；
  2. 设置页填入你的真实 API Key（DeepSeek/智谱/混元等），打开任一英文网页点「译」，确认译文出现；
  3. 或运行 `node tests/browser/real-extension-smoke.mjs`（自动冒烟：加载扩展 → 无 Key 引导 → 切 Google 真实翻译）。
- 完成上述冒烟后即可 `npm run zip` 打包发布 v0.1.23。
