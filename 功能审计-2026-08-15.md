# 好翻（open-translator-cn）功能逐条审计

> 当前版本：**v0.1.23**（2026-08-21 复核） · 原始审计基准：v0.1.22（2026-08-15/16） · 范围：对照截图特性表（17 项）核对源码
> 校验门禁（本地全绿）：版本一致 0.1.23 · 类型检查通过 · **42/42 单元测试通过** · ESLint 0 错误 0 警告 · 构建 310.74 kB
> 历史：v0.1.22 在 2026-08-15 审计基础上本地补全 8 项特性（方案 A：修代码补齐），特性表 17 项全部与实现对齐；2026-08-16 上线前安全复查 + 合并远程 v0.1.21 + 创建 Release v0.1.22。本节为持续审计档案。

## 结论先行

- ✅ **已实现并正常运行：17 项全部到位**。其中本次本地补齐的 8 项为：上下文感知(#3)、译文可编辑+术语自学习(#8)、多引擎智能路由(#11)、可拖动工具栏(#13)、流式输出(#2)、翻译质量自检(#4)、页内快速设置窗(#14)、翻译进度 X/Y+旋转(#15)。
- 之前"4 项描述夸张 + 4 项压根没做"的问题已全部消除；软件仍保持稳定、可装、可翻、无运行时崩溃 bug。

---

## 一、本地校验门禁（证明"没引入新 bug"）

| 步骤 | 命令 | 结果（v0.1.22 基准） | 结果（v0.1.23 复核） |
|---|---|---|---|
| 版本一致性 | `check:version` | ✅ 0.1.22 一致 | ✅ 0.1.23 一致 |
| 类型检查 | `tsc --noEmit` | ✅ 无错误 | ✅ 无错误 |
| 单元测试 | `test` | ✅ 39/39 通过 | ✅ **42/42 通过** |
| 代码规范 | `eslint .` | ✅ 0 错误 0 警告 | ✅ 0 错误 0 警告 |
| 构建 | `wxt build` | ✅ 310 kB 级（含 postbuild） | ✅ 311.25 kB |
| 浏览器回归 | `test:browser` | —（v0.1.22 未纳入门禁清单） | ✅ **32/32 通过** |

> v0.1.23 门禁于 2026-08-21 起逐步重跑全绿：新增 2 个单测（句子缓存批量合并语义）后 41/41；P2 修复后新增 1 个单测（批量恢复预算约束）达 **42/42**；2026-08-22 复审修复后浏览器回归 **32/32**；`typecheck` 与 `lint` 全绿，无新增告警。

---

## 二、17 项逐条核对

图例：✅ 已实现且无 bug · 🆕 本次本地新补齐

| # | 特性 | 实现位置 | 说明 |
|---|---|---|---|
| 1 | 沉浸式双语对照 | `utils/content-ui.ts` `createTranslationNode` + `entrypoints/content.ts` `insertTranslation` | 译文用 `<span>+shadow>` 插在原文下方，随页面滚动；普通流 `afterend`，Flex/Grid 末尾追加，避免布局错乱。 |
| 2 | 🆕 流式输出（极低首字延迟） | `entrypoints/background.ts` `setupStreamingPort`（`haofan-stream` 长连接端口）+ `utils/translator.ts` `translateOneStream`（`callChatStream` 增量回调 + `restorePartial` 占位符还原）+ `entrypoints/content.ts` `streamTranslateOne`/`translateOneText` | 内容脚本**首次真正**经 `runtime.connect({name:'haofan-stream'})` 长连接端口与后台通信（此前生产环境从不接通，流式为空）；后台用 SSE 把 token 逐块回推，内容脚本对四条单条路径（划词浮层 / 悬停气泡 / 点段落 / 输入框）**逐字渲染**。端口不可用 / 空结果 / 15s 空闲超时一律回退普通 `TRANSLATE_ONE`，保证不漏译。整页翻译仍走 `TRANSLATE_BATCH`（更省 Token，不改为逐条流式）。 |
| 3 | 🆕 上下文感知翻译 | `utils/translator.ts` `TranslationContext {title, previous}` + `defaultSystem` 追加【上下文】+ `entrypoints/content.ts` `buildPageContext` | 内容脚本构造 `{ 页面标题, 紧邻前段译文 }` 经 `TRANSLATE_BATCH`/`TRANSLATE_ONE` 的 `pageContext` 透传，系统提示据此保持术语与语气一致。 |
| 4 | 🆕 翻译质量自检（数字/URL/代码保真） | `utils/translator.ts` `auditTranslation`（正则校验原文数字/URL/代码 token 是否保留于译文）+ `translateOneDetailed`/`translateBatchDetailed`/`translateSentences`/`translateLongItem` 中的 `qualityCheck` 分支 + `utils/content-ui.ts` `🚩 已校正` 标记 | 译文漏掉数字/URL/代码符号时 `auditTranslation` 标记 issue，缺失项追加保真指令**单句校正重试一次**；仍缺失则保留首次结果并在 `🚩` 标记中提示，不误导。原 `utils/fidelity.ts` 为**重复实现的死代码，已于 v0.1.23 删除**，质量自检统一收敛到 `auditTranslation`。 |
| 5 | 可见区域优先（懒加载） | `entrypoints/content.ts` `ensureViewportObserver`（`IntersectionObserver`，rootMargin 320px） | 首屏视口内文本入首块，其余进观察队列，进入视口才翻译。 |
| 6 | 动态内容自动翻译 | `entrypoints/content.ts` `MutationObserver`（含 click-scan 兜底） | 无限滚动、Tab 切换、显隐均触发重新扫描并懒翻译。 |
| 7 | 划词翻译（Selection 浮层） | `entrypoints/content.ts` 选区 UI | 选区触发圆形"译"按钮→浮层显示译文+复制。 |
| 8 | 🆕 译文可编辑 + 术语自学习 | `utils/content-ui.ts` `createTranslationNode`（`contenteditable`）+ `entrypoints/content.ts` `learnTerm`/`learnGlossaryTerm` | 译文 hover 即可改；blur 比对基线译文，确有改动才触发；短术语（≤30 字无句末标点）自动沉淀进个人术语表并即时生效。 |
| 9 | 图片翻译（OCR + 覆盖层） | `utils/vision.ts` `translateImage` + `utils/image-overlay.ts` + `entrypoints/background.ts` `TRANSLATE_IMAGE` | 右键图片→后台重下载（规避 CORS）→视觉模型识别区域+译文→页面图片旁叠加框与结果面板；上传图片走独立结果页。 |
| 10 | 按网站暂停/恢复 | `utils/site-policy.ts` + 弹窗开关 + `entrypoints/content.ts` `setSiteDisabledState` | 弹窗"当前网站翻译"开关即时暂停/恢复，清理译文、移除悬浮按钮；`SITE_POLICY_CHANGED` 消息联动。 |
| 11 | 🆕 多引擎智能路由（备用引擎故障转移 + 长文强模型） | `utils/config.ts` 新增 `fallbackEnabled/fallbackProvider/fallbackApiKey/fallbackBaseUrl/fallbackModel/longTextModel` + `utils/translator.ts` `fallbackConfigOf`/`callChatRouted` + `utils/ui.ts` 开关 | 单段 ≥1800 字自动改用"长文强模型"；主引擎报 401/403/quota/额度等故障时切到备用引擎一次；设置页可配。 |
| 12 | 节省 Token（5 子项） | `utils/translator.ts` `translateBatchDetailed` + `cache.ts` + `usage.ts` + `utils/config.ts` `glossaryInjectionLimit` + `utils/ui.ts` | 句子缓存命中 0 Token、去重、术语命中零请求、目标语言跳过、术语注入上限（**现用户可在「节省 Token」区配置 0–60，默认 24**），五项全部到位。 |
| 13 | 🆕 可拖动工具栏 | `entrypoints/content.ts` `mountToolbar`（[译 + ⚙] 容器）+ `utils/storage.ts` `toolbarPosItem` | pointer 拖拽移动工具栏，位置存 `toolbarPosItem` 持久化；⚙ 打开页内快速设置；SPA 自愈保留。 |
| 14 | 🆕 页内快速设置窗（即改即生效） | `entrypoints/content.ts` `toggleSettingsPopover`（Shadow DOM 浮层复用 `buildConfigForm`） | ⚙ 在网页内弹出设置浮层，复用 Options/Popup 共用表单并注入表单 CSS，改动即时落盘生效；外部点击/Esc 关闭。 |
| 15 | 🆕 翻译进度显示（X/Y + 旋转） | `entrypoints/content.ts` `showProgress`（`已译 N / total 段`）+ `styles/content.css` `.ot-spinner` | 扫描结束令 `totalSegCount = foundCount`，进度分母真实；加载态带旋转动画。 |
| 16 | 键盘快捷键（Alt+T，可自定义） | `wxt.config.ts` `suggested_key` + `entrypoints/background.ts` `onCommand` | 复用右键"翻译本页"注入逻辑；浏览器设置页可改键位。 |
| 17 | 自定义能力（端点/Key/模型/系统提示/术语表/风格） | `utils/config.ts` `AppConfig` + `utils/ui.ts` `buildConfigForm` | 设置表单暴露全部可改项，即存即用（`storage.watch` + `getCfg()` 重读）。 |

---

## 三、安全性 / 可靠性复核（无新风险）

- API Key 仅存 `storage.local`，发送前 `cleanSecret` 剥离空白/控制符/非 ASCII；非法 Key 直接报错。
- 译文仅通过 `textContent` 注入，待译文本用 `<<<TRANSLATE_DATA>>>` 边界包裹 + 指令护栏防 Prompt Injection。
- 流式：仅在 LLM 引擎下走 `haofan-stream`；MT 引擎或端口异常时回退普通批，不会卡首段。
- 术语自学习：仅短源短语（≤30 字、无句末标点）写入术语表，避免把整句污染 glossary。
- 多引擎路由：仅主引擎确属「引擎整体不可用」（401/403/quota/额度/rate-limit）才切备用引擎一次，可恢复错误不误判。
- 空译文安全：返回空→`markTranslated`+跳过，不会把页面清空。
- MV3 service worker 回收：统计 `recordUsage` 串行队列持久化；流式用 `onConnect` 端口规避 `sendMessage` 无流式限制。

---

## 四、本次变更清单（本地，未上传）

新增（v0.1.22）：`utils/fidelity.ts`（质量保真比对，**已于 v0.1.23 删除**，合并进 `auditTranslation`）。
修改：`utils/config.ts`（多引擎路由配置）、`utils/requester.ts`（`streamChatCompletions`）、`utils/translator.ts`（`TranslationContext`/`callChatRouted`/`runLlmWithFidelity`/导出 `callChat`）、`utils/content-ui.ts`（`contenteditable` + 已校正标记）、`utils/ui.ts`（备用引擎/长文模型开关）、`utils/storage.ts`（`toolbarPosItem`）、`entrypoints/background.ts`（`haofan-stream` 端口 + `pageContext` 透传）、`entrypoints/content.ts`（流式首块、上下文、可拖动工具栏、页内设置、进度 X/Y）、`styles/content.css`（`.ot-spinner`）。

### 2026-08-15 末轮补充修复（对照官网描述的两处遗漏）

1. **🔧 术语注入上限可调（特性 #12「节省 Token」子项）** — 官网声明"术语注入上限可调"，但原实现 `relevantTerms(texts, target, custom, merged, limit=24)` 的 `limit` 为硬编码常量，用户无法配置。
   - `utils/config.ts`：`AppConfig` 增加 `glossaryInjectionLimit: number`，`DEFAULT_CONFIG` 默认 `24`。
   - `utils/ui.ts`：在「节省 Token」分区新增数字输入框（0–60，默认 24），并接入 `fill()`/`save()` 与事件监听。
   - `utils/translator.ts`：将 `cfg.glossaryInjectionLimit || 24` 接入全部 4 处 `relevantTerms(...)` 调用的 `limit` 参数（`translateOneDetailed`/`translateLongItem`/`translateItemsIndividually`/`translateGroup`），真正生效。
2. **🔧 流式首段也走多引擎路由** — 原流式通道 `background.ts` `onConnect` 直接调用 `callChat`，绕过了 `callChatRouted`（长文强模型 + 故障转移），仅尾部/普通批用了路由。现已改为 `callChatRouted`，首段同样享受长文路由与备用引擎故障转移。
   - 配套修正：`utils/translator.ts` 的 `callChatRouted` 改为 `export`（原为内部函数），`background.ts` 导入同步更新、移除未使用的 `callChat` 导入。

### 站点文案一致性复核（结论：无需改动）

- 复核结果：`features.html` 顶部统计数字实为 `data-count="30">30`（"30 天译文默认缓存"，见第 96 行）、`privacy.html` 两处均为 **30 天**、`cache.ts` 代码默认值 **30 天**——三者本就一致。
- 此前审计草稿中"features.html 写 25 天"为笔误，特此更正；站点文案与代码无矛盾，无需任何改动。

### 2026-08-16 上线前安全复查（全量代码审查 + 修复）

- 派专审子代理全量扫描 `entrypoints/` + `utils/`，逐类核查 XSS / unsafe DOM 注入、API Key 泄露、Prompt Injection 覆盖、网络超时与重试、错误处理、MV3 service worker 生命周期、稳定性边界。
- **确认干净的项**：所有译文落点均用 `textContent`（无 XSS）；API Key 仅经 `Authorization` 头、存 `storage.local`、无 console 泄露、无 URL 携带；网络层 `AbortController`+超时、仅重试 429/5xx/timeout 且上限 1 次、SSE 流 `finally` 内 `reader.cancel()` 清理；MV3 `onConnect`/`onMessage` 顶层注册、端口断连 `controller.abort()`；缓存 2000 条 LRU 上限；`MutationObserver` 排除自身注入节点防递归。
- **修复 #1（中危 · Prompt Injection）**：原 `defaultSystem` 把不可信的页面 `title`/前文直接拼进**系统提示词**，恶意网页可借 `<title>` 注入指令操纵译文。改为 `pageContextBlock()` 把上下文作为「被显式降级为数据、不是指令」的区块追加到**用户消息**末尾，并截断（标题≤160 / 前文≤200 字）、剔除换行；`defaultSystem` 不再接收 `context`。新增回归测试守护（页面标题含注入指令时只出现在用户消息、不进系统提示词）。
- **修复 #2（低危 · 数据完整性）**：`learnGlossaryTerm` 写入前未净化，源词含 `=`/换行会破坏术语库行结构。已净化：剔除换行、把源词分隔符 `=`/`＝` 换成 `-`，空值跳过。
- **保留不改（低危 · 非阻塞）**：批量坏 JSON 恢复时 `recovery=true` 会短路预算判断，使 `MAX_BATCH_RECOVERY_REQUESTS` 常量实际不生效；但递归靠 `recovery=true→translateItemsIndividually` 必然终止，不会死循环，不影响安全性与正确性，留作后续优化。
- 复查后重跑门禁全绿：30/30 测试、typecheck 0 错、lint 0 警告、build 215.57 kB。

版本号维持 **v0.1.22**（按要求仅本地补齐，未 bump）。

### 2026-08-16 推送前：合并远程 v0.1.21 并整合（最终发布形态）

- **背景**：本地 v0.1.22 基于 v0.1.4 并行开发；远程 `origin/main` 已从 v0.1.4 演进到 **v0.1.21**（42 个提交，含流式/进度X·Y/注入防护+上下文降级/可拖动工具栏/图片翻译/坏JSON回退等，改动量约为本地的 2 倍，并带浏览器回归测试与 `postbuild`）。直接推送被拒（非快进）。经对比确认两边为**互补**而非替代。
- **用户决策**：合并并保留远程更成熟的扩展源码。执行 `git merge -X theirs origin/main`（冲突区采用远程，非冲突区双方皆保留），生成合并提交 `bbce132`，不丢任何工作。
- **本地独有、已保留的贡献**：
  1. 整套官网 `docs/`（index/features/privacy/faq/install/providers）—— 软件要"匹配"的对象，远程原本完全没有；
  2. `sendRuntimeMessage` 90s 兜底超时（防后台 Worker 挂起时翻译静默卡死）—— 合并后仍在 `entrypoints/content.ts`（非冲突区，保留）；
  3. 术语注入上限可调 —— 远程已有等价能力 `glossaryTermLimit`（默认 12，带 `<select>` UI 并接入 `relevantTerms`）。本地原先并行的 `glossaryInjectionLimit` 因此成为**冗余且会崩溃的死代码**（`ui.ts` 对其 `querySelector('[data-f=glossaryInjectionLimit]')` 取到 `null`，保存时 `null.value` 抛错）。已**清理**该死代码，统一使用远程可用的 `glossaryTermLimit`，官网"术语注入上限可调"声明由之满足。
- **版本**：`package.json` 统一为 `0.1.22`（远程为 0.1.21），与 `wxt.config.ts`（无显式 version，沿用 package.json）及构建产物 manifest 一致；`check:version` 门禁校验。
- 合并后重跑全量门禁**全绿**（check:version 0.1.22 / typecheck 0 错 / lint 0 警告 / **测试 39/39** / build 304.46 kB 含 postbuild 修正 popup/options/image-translate HTML），重新打 zip（Hao-Fan-0.1.22.zip，26 文件）。
- **已成功推送到 GitHub**：`00d13c9..d946941 main -> main`（fast-forward，未强制推送，未丢失任何远程工作）。注意 GitHub 提示仓库已更名，本地 remote 已更新为 `https://github.com/Lokeily/hao-fan.git`（原带尾杠的 `Hao-Fan-` 为废弃重定向地址）。


### 2026-08-16 创建 GitHub Release v0.1.22 并上传 zip

- **触发**：用户确认"肯定要把 releases 上传，V0.1.22 的版本得传"。
- **认证方式**：本机无 `gh` CLI、无环境变量 token；使用 macOS keychain 中既有的 `github.com` 凭证（即此前 `git push` 所用的 PAT，40 位，身份 `Lokeily`）。凭证**仅经内存变量传入 GitHub REST API，全程未打印到对话**。
- **操作**（GitHub REST API，非强制、非草稿）：
  - 创建 Release：`POST /repos/Lokeily/hao-fan/releases`，`tag_name=v0.1.22`、`target_commitish=main`、`name=v0.1.22`、附发布说明（稳定性修复 / 安全审查 / 质量保障 / 安装方式）。tag 不存在时由 API 在 main 头部自动创建。
  - 上传附件：`POST uploads.../releases/{id}/assets?name=Hao-Fan-0.1.22.zip`，`Content-Type: application/zip`，135016 字节。
- **结果**：
  - Release 页面：`https://github.com/Lokeily/hao-fan/releases/tag/v0.1.22`（release id `371200981`）
  - 下载地址：`https://github.com/Lokeily/hao-fan/releases/download/v0.1.22/Hao-Fan-0.1.22.zip`
- **安全提示**：所用 PAT 来自用户本地 keychain，未在对话中明文出现，因此无需按"对话中出现即吊销"的规则强制轮换；用户若想收紧权限可随时在 GitHub 后台轮换该 token。

---

### 2026-08-21 · v0.1.23 P0/P1 修复与易用性优化（本轮）

> 目标：在 v0.1.22 已发布形态上，先修 P0/P1 紧急重要项，再做一轮自检确认无 bug、无遗漏。
> 门禁（本轮重跑）：`check:version` 0.1.23 一致 · `typecheck` 0 错 · `lint` 0 警告 · **`test` 41/41** · `build` 310.75 kB。

#### P0-1 · 真正接通流式输出（内容脚本侧 `runtime.connect`）

- **根因**：v0.1.22 后台已注册 `haofan-stream` 长连接端口，但内容脚本四条单条交互路径（划词 / 悬停 / 点段落 / 输入框）从未调用 `runtime.connect`，生产环境流式实际为空，单条翻译只能等整块返回，首字延迟高。
- **修复**（`entrypoints/content.ts`）：
  - 新增流式客户端：`getStreamPort()` 懒连接 `runtime.connect({name:'haofan-stream'})`；`streamUnavailable` 标志在端口异常时置位，避免反复重连；`streamWaiters`（`id → waiter`）收集在途请求，断连时统一 reject。
  - `STREAM_IDLE_TIMEOUT_MS = 15_000` 空闲超时；`armStreamTimer` 在收到 delta 时续命。
  - `streamTranslateOne(text, onDelta, options)`：开关关闭或端口不可用返回 `null`；否则发 `{type:'translate-one', id, text, jobId, context}`，逐块调 `onDelta` 做增量渲染。
  - `translateOneText(text, options)`：能流式就流式，**否则回退普通 `TRANSLATE_ONE`**——保证任何异常都不漏译。
  - 划词浮层 / 悬停气泡 / 手动点段落 / 输入框四处单条交互改为 `translateOneText(..., { onDelta })`：先占位显示增量译文，最终译文覆盖；新增 `updateSelectionResultText` 只改 `.result` 文本、不重建面板；`dropTranslationNode` 在流式中途失败时撤掉半截译文。
- **后台配套**（`entrypoints/background.ts` `setupStreamingPort`）：done 消息补上 `stats: { estimatedTokensSaved }`，使流式路径的缓存命中也计入「已省 Token」（此前漏计）。
- **占位符半截断**：`utils/translator.ts` `translateOneStream` 循环内 `onDelta(restorePartial(masked, full))`，增量也经 `restorePartial` 还原遮罩字符（`OPEN/CLOSE` 未闭合时先裁掉尾部），避免私有区字符闪现到界面。

#### P0-2 · 质量自检对齐（删死代码 `fidelity.ts`）

- **根因**：`utils/fidelity.ts`（`fidelityIssues`/`fidelityInstruction`/`runLlmWithFidelity`）与 `translator.ts` 的 `auditTranslation` 功能重复，且前者从未被任何路径调用（grep 全仓零 import），是无用死代码，反而让审计与实现脱节。
- **修复**：`git rm utils/fidelity.ts` 并 `npx wxt prepare` 重新生成类型；质量自检统一由 `auditTranslation`（正则校验原文数字/URL/代码 token 是否保留于译文）承载，覆盖 `translateOneDetailed`/`translateBatchDetailed`/`translateSentences`/`translateLongItem` 的 `qualityCheck` 分支。
- **验证**：`auditTranslation` 在 6 处被调用；单测 `ok 30 - 质量自检：数字与 URL 缺失时标记 issue` 守护该行为。

#### P1 · T1 句子缓存 miss 改为批量请求（省 Token）

- **根因**：`translateSentences` 中每句缺失都串行调一次 `coreTranslate`，N 句缺失 = N 次请求，每次都重复发送 system + 术语 + 上下文前缀，浪费 Token。
- **修复**（`utils/translator.ts` `translateSentences`）：缺失句子合并为**一次** `translateBatchDetailed`；新增 `mergeSubStats`（不叠加 `sentSegments`/`sentCharacters`，避免重复计数）；把句子路径此前丢弃的 `issue` 汇总回传（`auditTranslation` 缺失 token 标记）。
- **验证**：单测 `ok 37`（缩写不拆散 + 缺失合并为 1 次请求）、`ok 38`（仅一句变化只送 1 条 item）守护。

#### P1 · R1 动态重译加冷却 + 数字跳过（省 Token / 防烧）

- **根因**：SPA 中股票行情/时钟每秒刷新，原文仅数字变化时 `MutationObserver` 触发整段重译，持续烧 Token。
- **修复**（`entrypoints/content.ts` `refreshChangedText` 重写）：
  - 同源直接 return；**仅数字变**则 `digitSkeleton` 抹数字/空白比对 + `patchTranslationNumbers` 就地换数（0 请求）。
  - `RETRANSLATE_COOLDOWN_MS = 8_000` + `lastRetranslateAt` WeakMap + `cooldownQueue`/`flushCooldownQueue`/`queueAfterCooldown`：冷却期内抖动合并，到点再 `release`+`scheduleRoot`。
- **验证**：类型/lint 通过，未引入新告警；逻辑路径覆盖「同源 / 仅数字变 / 冷却排队 / 真实重译」四类。

#### P1 · U2 首次无 Key 引导 + 默认 manual（新用户友好）

- **根因**：新用户未填 API Key 时点「翻译本页」会发无效请求并可能报错，第一印象差。
- **修复**（`entrypoints/content.ts` + `utils/storage.ts`）：
  - 新增 `providerNeedsSetup(cfg)`（判断 Key/端点是否缺失）、`needsSetupNow()`、`showSetupGuide(force)`（一次性引导卡，可一键开设置）。
  - `translatePage(initial, userInitiated)` 开头若 `needsSetupNow()` 则 `awaitingSetup=true; showSetupGuide(userInitiated); return`，**不发任何请求**。
  - `utils/storage.ts` 新增 `setupNoticeShownItem`（`local:setupNoticeShown`，默认 false）：引导卡仅打扰一次。
  - config watch 内：填好 Key 后 `awaitingSetup && !providerNeedsSetup(...)` 则自动 `awaitingSetup=false; closeNotice(); translatePage(true)` 接着翻。
- **验证**：`showSetupGuide`/`providerNeedsSetup`/`needsSetupNow` 均接入；`setupNoticeShownItem` 导入并落盘。

#### 本轮自检结论

- ✅ 门禁全绿（version / typecheck / lint / test 41/41 / build），**无新增 bug、无新增告警**。
- ✅ 流式真正接通（内容脚本 `runtime.connect` + 增量渲染 + 回退）；质量自检收敛到 `auditTranslation` 且仍正常。
- ✅ T1 句子批量省 Token、R1 动态重译冷却、U2 无 Key 引导三项 P1 均生效。
- ⚠️ 版本号维持 **v0.1.23**（本地修复，未 bump 至 v0.1.24、未推送）。如需发布请执行 `wxt build` 后打 zip 并创建 Release。
- 注：`Hao-Fan-0.1.22.zip` 为上一版产物，v0.1.23 发布前需重新构建打包。

### 2026-08-22 · P2 修复（审计「保留不改」项收尾）

> 用户确认 P2 范围为：审计中唯一标记「保留不改 / 留作后续优化」的 1 项 —— 批量坏 JSON 恢复时 `MAX_BATCH_RECOVERY_REQUESTS` 形同虚设。现已修复并回归。
> 门禁（重跑）：`check:version` 0.1.23 一致 · `typecheck` 0 错 · `lint` 0 警告 · **`test` 42/42（+1 新用例）** · `build` 310.74 kB。

#### 根因

`translateBatchDetailed` 的恢复逻辑用 `recovery=true` 布尔标志：顶层批次失败后拆半，两个子组均以 `recovery=true` 递归。子组若再失败，会立刻命中 `if (recovery || recoveryRequests + 2 > MAX_BATCH_RECOVERY_REQUESTS)` 走逐条兜底——**子组永远不再拆分**，`recoveryRequests` 永不累加，`MAX_BATCH_RECOVERY_REQUESTS` 形同虚设：无论常量设 1 还是 100，行为都相同（只允许 1 层拆分）。因 `recovery=true → translateItemsIndividually` 必然终止，故无死循环风险，但常量语义失效。

#### 修复（`utils/translator.ts` `translateGroup`）

- 用 **`splitsLeft`（剩余拆分预算）** 替代布尔 `recovery`：顶层默认 `splitsLeft = MAX_BATCH_RECOVERY_REQUESTS`，每向下拆一层减 1。
- 多条目失败时：`splitsLeft > 0` → 拆半递归（子组 `splitsLeft - 1`）；`splitsLeft <= 0` → 逐条兜底（必然终止）。
- 单条目失败时：仅顶层（`splitsLeft === MAX`）复用原响应文本（兼容模型直接回纯文本，省 1 次请求）；拆分产生的单条目 → 走普通单句提示，避免把模型解释/拒答当译文。
- 常量现在真正生效：`值 = 允许的最大拆分层数`（默认 2 层）。
- 移除不再使用的 `recoveryRequests` 计数器。

#### 新增回归测试（`tests/translator-network.test.mjs`）

- `批量坏 JSON 恢复受 MAX_BATCH_RECOVERY_REQUESTS 约束（不无限拆分）`：4 条目批次始终返回坏 JSON，断言组请求恰为 7 次（1 顶层 + 2 第一层 + 4 第二层，预算 2 层）——旧实现只有 3 次，此用例可直接锁定回归。

#### 结论

- ✅ P2 唯一遗留项已修复；门禁 42/42 全绿，无新增告警；行为更可控（恢复深度有界且受常量约束）。
- ⚠️ 版本号维持 **v0.1.23**（未 bump、未推送）。发布前需重新 `npm run build` 打包。

---

### 2026-08-22 · 全量复审（bug / 应用问题 / 配置 / UI 四类）

> 用户要求：重新审计软件，检查 ① bug ② 新出现的应用问题 ③ 配置问题 ④ UI 显示问题，自行检查并修复后汇报。
> 门禁（修复后重跑）：`check:version` 0.1.23 一致 · `typecheck` 0 错 · `lint` 0 警告 · **`test` 42/42** · **`test:browser` 32/32** · `build` 311.25 kB。

#### 发现并修复的问题

1. **🔧 配置问题（存在数据丢失风险，v0.1.22 起即存在）— `utils/ui.ts` `fill()` 不回填 4 个高级字段**
   - 根因：`fill()` 只回填表单主字段，`fallbackProviders` / `strongProvider` / `strongModel` / `strongThreshold` **只写不读**。打开设置页时这 4 项显示空值/旧值；用户改任意其它项保存，`save()` 会把它们读成空 → **静默清空多引擎故障转移与长文强模型配置**。
   - 修复：`fill()` 补 4 处 `setIfDiff` 回填（含 `strongProvider` 选项存在性守卫，避免写入列表外的值）。
2. **🔧 新应用问题（v0.1.23 引入的浏览器回归）— 无 Key 引导门导致 10 个浏览器回归用例失败**
   - 根因：U2 的无 Key 闸门使 `translatePage` 在无 Key 时拦截、不发请求；但 `tests/browser/browser-mock.js` 从不注入带 Key 的 `config`，mock 又始终能返回译文（代表"已配置用户"）。依赖整页翻译的用例（两列/多列/网格/取消任务/自动翻译/perf 等）全部被拦下。
   - 修复：`browser-mock.js` 预置一份带 Key 的 `config`（测试环境 = 已配置用户，与"mock 总能返回译文"语义一致）；`auto-translate` 用例移除 `config: undefined`（否则会清掉预置 Key）。
   - 附带发现：浏览器测试加载的是**构建产物** `.output/chrome-mv3/content-scripts/content.js`，改源码后必须 `npm run build` 再跑 `test:browser`，否则测的是旧 bundle（本次已踩坑并记录）。
3. **🔧 新应用问题（U2 只做了一半）— 无 Key 闸门未接入单条交互路径**
   - 根因：U2 只把引导拦在了整页 `translatePage`；划词 / 悬停 / 点段落 / 输入框四条单条路径仍会发无效请求并弹「翻译失败」错误提示，与"无 Key 不发任何请求 + 引导"的目标不一致。
   - 修复：`entrypoints/content.ts` 新增统一闸门 `guardSetupGate(userInitiated)`，接入四处单条路径——主动操作（划词/点段落/输入框）`force=true` 必弹引导，悬停被动触发只打扰一次；被拦下即 return，不再发请求。
4. **✅ 未发现新增 UI 显示问题**：`createNoticeHost` 双按钮（primary/secondary）深浅色、`createTranslationNode` 译文样式与 `is-pending` 占位动画、流式增量占位、质量自检 `data-quality="warn"` 标记、设置面板深浅色均正常；未发现样式/布局/主题回归。
5. **✅ 近期改动热点复审无 bug**：流式长连接客户端（端口生命周期/15s 空闲超时/断连 reject/回退）、`restorePartial` 占位符增量还原、`translateSentences` 批量合并 + `mergeSubStats`、`refreshChangedText` 数字补丁 + 8s 冷却、`translateGroup` 恢复预算——逐行核对未见逻辑错误。

#### 新增回归测试

- `tests/browser/extension-regression.spec.ts` → `no API key: translation is gated by the setup guide and sends no requests`：手动模式 + 无 Key 下，点段落与整页翻译均被引导卡拦下且 `data-translation-calls` 保持 0（守护 U2 完整语义：整页 + 单条路径都拦截、零请求）。

#### 结论

- ✅ 四类问题全部排查：修复 3 个（配置回填、浏览器回归、单条路径引导缺失），UI 与热点逻辑无新增问题；全门禁绿（含浏览器回归 32/32）。
- ⚠️ 版本号维持 **v0.1.23**（未 bump、未推送）。发布前需重新 `npm run build` 打包；浏览器测试需在 build 后运行。

---

### 2026-08-22 · 软件验证（可行性 / 安全性 / 稳定性）

> 用户要求按合格程序员的验证方法继续验证。完整报告见 `验证报告-2026-08-22.md`，摘要如下。

#### 可行性
- 双平台构建（chrome-mv3 311.25 kB / firefox-mv2 311.32 kB）与商店 zip（chrome 117.87 kB / firefox 117.93 kB + sources 510.32 kB）全部成功且 `unzip -t` 无损。
- 双平台 manifest 审计通过：权限最小、CSP 严格、gecko ID 稳定、数据收集声明准确。
- 真实 Chrome `--load-extension` 冒烟：本沙箱环境不支持（无头/沙箱限制），已备 `tests/browser/real-extension-smoke.mjs` 供本机桌面 Chrome 运行。

#### 安全性
- 仓库工作区 + git 全历史密钥扫描：0 残留（从未提交过密钥）。
- Key 仅存 storage.local、仅走 Authorization 头（Bearer / DeepL-Auth-Key / 订阅 Key 头）、无 URL 携带、`cleanSecret` 净化（单测 ok 41/43/44 守护）。
- 全仓零 `innerHTML`/`insertAdjacentHTML`/`document.write`/`console.log`；译文全 `textContent` + Shadow DOM，无 XSS 面。
- Prompt Injection：`<<<TRANSLATE_DATA>>>` 边界 + `INJECTION_GUARD` + 上下文降级到 user 消息（单测 ok 32 守护）。

#### 稳定性
- 单测 **45/45**（新增 3 个 MT 引擎用例：Google/DeepL/Microsoft）、浏览器回归 **32/32**、typecheck/lint/check:version 全绿、perf 基准 2001 段首译 90ms 无长任务卡顿。
- 竞态/并发/超时/恢复均有专项守护（取消 jobId 守卫、并发 ≤2、串行写队列、流式 15s 空闲超时、坏 JSON 预算恢复）。

#### 本轮修复/补强
- 修复：`translateMT` google 端点 URL 硬编码 → 改为 `cfg.baseUrl || 默认`（与 DeepL/Microsoft 一致，生产行为不变），使其可被测试重定向。
- 补测：MT 引擎路径此前零覆盖，新增 3 单测；测试 mock 补记 `headers`。
- 新增 `tests/browser/real-extension-smoke.mjs`（桌面 Chrome 真实加载冒烟脚本，本环境不可执行）。

#### 结论
- ✅ 三维度验证全部通过，无高危问题，达到可发布水平。
- ⚠️ 唯一待办：本机桌面 Chrome 用真实 API Key 做一次端到端冒烟（沙箱无法访问外部翻译服务与图形化扩展加载）。完成即可发布 v0.1.23。
