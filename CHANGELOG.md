# 更新日志（Changelog）

## v0.1.10 (2026-08-15)

- 修复：悬停气泡/设置面板白底看不清——宿主带 all:initial !important 时
  :host 规则会被内联样式覆盖，背景变透明导致白底白字。改为全局主题工具
  （不透明配色 + JS 直接内联，深浅色跟随系统），任何网页上都清晰可读。
- 新增：页面内完整设置大面板——悬浮设置点击「打开完整设置」即在页面内
  弹出大面板（iframe 加载扩展设置页），右上角弹窗的全部设置内容都在这里，
  可拖动、可关闭，无需跳转标签页。
- 测试：新增设置面板语言/引擎切换持久化与完整设置面板用例（26 全过）。

## v0.1.9 (2026-08-15)

- 修复：译文脉冲闪烁——流式译文到达后未移除占位脉冲动画（is-pending），
  现已在上传译文的三处更新路径中移除。
- 修复：悬浮设置面板 / 悬停气泡 / 输入框结果深色模式失效——宿主背景
  内联写死白色无法被媒体查询覆盖，改为 :host 规则随系统深浅色。
- 修复：弹窗「大片空白 + 一小列文字」——构建产物页面资源为绝对路径 +
  crossorigin，部分 Chrome 环境会拦截导致 CSS 加载失败裸渲染。
  postbuild 改为相对路径并移除 crossorigin；popup 增加防御性内联样式与
  color-scheme meta（三页面）。
- 测试：新增 CSS 多列 / 网格卡片布局对齐回归（24 浏览器测试全过）。

## v0.1.8 (2026-08-14)

- 优化：翻译进度显示——状态条实时显示「已译 X/Y 段」（Y 为本次扫描段落总数）。
- 优化：悬浮工具栏深色模式适配（底色/齿轮颜色跟随系统外观）。
- 优化：译文插入路径样式计算减负（多列检测限 3 层），性能基线最长长任务 220→192ms。
- 提示：设置页与 README 补充 Alt+T 快捷键说明；README 功能表补全新交互。

## 未发布

- 新增：悬停翻译——鼠标悬停段落 500ms 即显示译文气泡（可固定/取消），命中会话缓存零请求。
- 新增：输入框翻译——网页输入框（textarea/文本输入）聚焦时显示「译」按钮，译文浮层带复制。
- 新增：译文显示样式——默认 / 蓝色虚线 / 下划线 / 高亮块四种，设置页切换即生效。
- 新增：自动翻译此站——站点在自动翻译列表时页面加载后自动开始翻译（设置面板可管理）。
- 修复：设置页 checkbox 切换时勾选样式不同步的回归（:has 兼容替代丢失）。
- 设置页新增「悬停翻译 / 输入框翻译 / 译文样式」控件。

## v0.1.7 (2026-08-14)

- 修复：句子缓存切分误拆英文缩写（U.S. / Dr. / e.g. 等被拆成单字母），已加缩写保护。
- 修复：流式翻译失败时不再静默——错误原因会提示用户，段落自动回到懒翻译队列重试。
- 修复：非流式批量翻译后同步更新上下文窗口（上一段译文），长文一致性更好。
- 兼容：开关勾选样式移除 :has() 依赖（旧浏览器），改由 JS 同步 class。
- 优化：网页端悬浮按钮、图片翻译面板统一苹果系统蓝 + 毛玻璃 + 深色模式适配。
- 安全：术语自动学习的抽取请求增加数据边界，防止网页文本注入指令。
- 修复：译文与原文对齐（核心体验）——
  · 双栏/多列/float 布局中译文不再被挤到一栏末尾或流入下一列，译文嵌入原文块内部跟随原文；
  · 无语义块的纯文本容器（div 内多行 span / 手写 HTML / 邮件排版）按行拆分翻译，
    每行译文紧跟对应原文行，不再整块并成一段；
  · 新增双栏与纯文本块两个浏览器回归测试。
- 新增：悬浮工具栏升级为可拖动的按钮组（译 + 齿轮设置入口），位置持久化到本地，
  可拖到任意位置不遮挡视野。
- 新增：页面内快速设置悬浮窗（目标语言 / 翻译引擎 / 暂停本站 / 打开完整设置），
  可拖动、记忆位置，点击齿轮按钮即开。
- 优化：设置页宽屏（≥1280px）双栏布局，充分利用大面积。
- 修复：翻译加载态恢复后按钮宽度丢失的历史隐藏 bug（工具条意外变窄）。
- 测试：新增拖拽持久化、设置面板、双栏对齐回归用例（20 浏览器测试全过）。
- 优化：Token 用量极致压缩——
  · 系统提示词/注入防护/批量协议/风格提示全部精简（每请求省 ~60-90 token）；
  · 术语注入默认 24 条降至 12 条，按长度升序（短术语优先），总字符 500 上限；
  · 上下文窗口截断收紧（标题 80 / 前段译文 160 字符），说明文案精简；
  · 质量自检校正重试文案压缩；
  · 设置页新增「术语注入上限」控件（0/6/12/24 条），缓存键同步隔离。
- 设置页「节省 Token」区重组：句子级缓存开关归位，布局更清晰。

## v0.1.6 (2026-08-14)

- 优化：401 / 403 认证错误给出明确中文指引（区分 Key 无效/过期与账户权限问题，附服务商原始返回），不再直出英文原文。

## 未发布

- 修复：流式翻译在后台 SW 休眠/扩展重载断开端口时永久挂起（busy 卡死）——增加 25s 超时与端口断开双兜底，未完成段落自动回到懒翻译队列补译。
- 修复：传统 MT 引擎（DeepL/Google/Microsoft）不再做无意义的 LLM 校正重试。
- 优化：设置页宽屏布局加宽至 980px，宽屏下字段支持三列。

## v0.1.5 (2026-08-14)

- 新增：流式输出（SSE）——首块边生成边显示，首字延迟降至首个 token 到达（基线：92ms）。
- 新增：上下文感知翻译——注入页面标题与前段译文的滑动窗口，长文代词指代与跨段术语更连贯。
- 新增：翻译质量自检——数字/URL/邮箱/占位符/代码 token 保真校验，缺失时自动校正重试并在译文上标记提示。
- 新增：句子级缓存与归一化匹配——SPA 内容微变时只重译变化的句子，句末标点/大小写/空白差异可命中。
- 新增：多引擎路由——备用引擎故障转移（主引擎 429/5xx 自动切换）与长文强模型路由（可配置阈值）。
- 新增：译文可编辑与术语自动学习——hover 译文即可修改，LLM 自动抽取调整的术语沉淀进个人术语表。
- 优化：全应用 UI 对标苹果（iOS Human Interface）——分组卡片、系统蓝、SF 字体栈、毛玻璃、iOS 开关与分段控件、完整深色模式。
- 测试：新增质量自检/句子缓存/上下文/流式的网络层用例；新增 2000 段重页面的性能基线采集（npm run perf）。
- 文档：新增 PRIVACY.md 与三商店上架清单（docs/STORE_SUBMISSION.md）。

## v0.1.4 (2026-08-14)

- 修复：CI 依赖审计 —— `brace-expansion` 升级至 5.0.9（规避新披露的高危漏洞 GHSA-rgw5-rvv9-x895），`nanoid` / `postcss` / `esbuild` 同步升至安全版本，`npm audit` 归零。
- 修复：浏览器回归测试同步 IndexedDB 迁移 —— mock 增加最小 IndexedDB 实现，图片结果页测试改从真实数据路径验证。
- 优化：图片翻译任务存储增加 24 小时过期清理与连接释放，未打开的结果页不再长期占用存储。
- 修复：图片翻译任务改用 IndexedDB 存储，规避 `storage.local` 在 Firefox（5MB）/ Chrome（10MB）下的配额限制，大图不再写入失败或与翻译缓存争抢配额。
- 修复：Google 翻译（免 Key）明确标注为非官方体验通道；端点异常时提示更稳定、需 Key 的替代服务。
- 安全：翻译与图片请求增加 Prompt Injection 防护，将待译文本标记为数据而非指令，降低恶意网页操纵译文的风险。
- 文档：仓库已重命名为 `hao-fan`；重写 README 为「简介 / 功能 / 安装 / 快速开始 / 隐私 / 开发」结构，同步更新链接与免 Key 端点稳定性说明。

## v0.1.3 (2026-07-27)

- 品牌：统一使用新的“文/译”双向翻译 Logo，并同步扩展清单图标与所有菜单页面品牌头部。
- 优化：弹窗主页增加字符计数、成功/失败/处理中状态、策略加载锁定和可键盘操作的标签切换，统计与图片入口在窄屏下更易扫描。
- 优化：设置页读取本地配置完成前保持表单不可编辑，避免默认值闪烁或误覆盖已有设置；独立设置页统一品牌头部与移动端间距。
- 优化：图片结果页统一加载、失效、存储异常、图片加载失败和零文本状态，增加文本数量摘要、译文列表和响应式双栏/单栏布局。
- 修复：取消整页翻译后立即重新开始时，旧任务不再错误清除新任务的加载与取消状态。
- 修复：站点暂停规则启动时发生并发更新，不再被较旧的初始读取结果覆盖。
- 修复：关闭划词浮层或暂停网站会真正取消后台翻译；取消消息之前已排队的迟到请求也不会继续调用接口。
- 修复：图片引擎不受支持或配置读取失败后会重置文件选择框，同一图片可直接重试，并阻止重复上传。
- 修复：设置保存失败时不再继续测试旧配置，避免显示误导性的连接结果。
- 新增：扩展弹窗可按当前网站暂停或恢复翻译；暂停会取消任务、清理译文并阻止网页与右键图片翻译，无需刷新即可恢复。
- 测试：新增整页、划词、动态内容、批量降级、取消重启、站点规则、划词取消与图片重试的自动浏览器回归，并接入 CI。
- 安全：覆盖构建链中的高危 `brace-expansion` 传递依赖，保持高危依赖审计通过。

## v0.1.2 (2026-07-23)
- 修复：整页 / 划词翻译在 `http://` 等非安全上下文站点崩溃（`crypto.randomUUID` 替换为不依赖安全上下文的 `crypto.getRandomValues` 实现）。
- 优化：点击扫描从「整页遍历」收敛为「点击元素子树有界扫描」，避免每次点击都遍历整棵 DOM 造成卡顿，Portal 菜单与显隐切换继续由 MutationObserver 接管。
- 修复：批量译文数量偶发不一致时改为逐条回退翻译，不再因单批异常中断整页其余内容的翻译。
- 修复：批量 JSON 连续解析失败时使用并发 2 的逐条兜底，避免整批漏译或请求瞬时爆发。
- 修复：逐条兜底保留取消、错误和重试语义，不再把失败原文误记为已翻译。
- 修复：动态内容监听覆盖 `aria-expanded`、`aria-controls`、`aria-owns` 与 `data-state`，已有弹层显隐后可继续翻译。
- 修复：测试连接强制绕过缓存、术语与目标语言本地跳过，确保真正访问当前服务商。
- 修复：同步 `package-lock.json`、README 版本与实际 GitHub 发布地址。

## v0.1.1 (2026-07-23)

### 改进
- 排除站点页眉、导航、菜单、工具栏和搜索区域，避免紧凑操作区被译文撑乱；表单及对话框正文恢复翻译。
- 译文继承原文对齐方式；动态页面响应回填前校验原文，避免旧译文插入已变化的内容。
- 扩展更新或重载后不再访问失效的运行时对象，相关错误改为提示刷新当前网页。
- 页面错误改为居中的可关闭蒙版，并按单次操作去重；配置及鉴权错误不再自动重复请求。
- 动态扫描继续遍历已翻译容器的后代，并监听文本替换、异步插入及可见性切换；点击后挂载的面板和无限滚动内容可继续按视口翻译。
- 译文改为绑定原文语义块的稳定槽位，不再越过共享裁剪容器，避免倒序、错位和覆盖下一节。
- 整页文本改为分帧扫描，首批可见段落无需等待全页分析；滚动内容按视口距离提前加载。
- 翻译失败的可见段落会重新进入观察队列，最多重试两次；无限滚动不再受生命周期总数上限影响。
- 批量响应兼容对象、字符串数组和 translations 数组，异常回退最多增加两个请求，不再逐段放大 Token。
- 默认提示词压缩；缓存按 API Base URL 和协议版本隔离；截断请求计入服务商返回的 Token 用量。
- 网络请求改为 20 秒超时、最多一次重试，区分 429/5xx 并支持 Retry-After 与可取消退避。
- 自定义 OpenAI 兼容接口可手动开启图片能力；图片结果截断或格式无效时明确报错。
- CI 同时生成 Chromium 与 Firefox ZIP，版本号同步为 0.1.1。
- 修复首屏批次受字符上限切分时遗漏可见段落的问题，并清理动态页面中已移除原文对应的译文状态。
- 划词翻译在请求前锁定语义锚点；图片结果的坐标层改为与原图同一容器，避免内边距导致偏移。
- 预设服务商支持填写新模型名；自定义接口的视觉开关在弹窗上传入口同步生效。
- 发布工作流拆分只读构建与标签发布权限，并修复开发工具链中的高危传递依赖。
- 修正智谱 OpenAI 兼容端点并自动迁移旧配置；更新腾讯混元模型预设和视觉能力标记。
- API Key 改为按服务商独立保存，并自动迁移旧版单 Key 配置。
- 整页翻译支持主动取消；取消后终止网络请求并丢弃过期结果。
- 新增消息边界、配置迁移、术语表、视觉响应与请求取消单元测试。
- 启用 TypeScript 严格模式，CI 增加测试、类型检查和 Firefox 构建。
- 新增高置信度目标语言本地跳过、重复文本合并统计和 Token 用量面板。
- LLM 批量翻译改用稳定 ID 的 JSON 协议，避免分隔符错位。
- 整页翻译改为首屏小批优先、后续按字符量大批并发；检测模型截断并自动拆分超长内容。
- DeepL / Microsoft 使用原生批量接口，Google 使用受控并发，批次统计写入不再阻塞页面渲染。
- 文本收集改为语义段落模型；译文使用 Shadow DOM 隔离站点样式，并规避表格、列表和裁剪容器错位。
- 译文改为继承原文字体与颜色的轻量第二行，移除卡片背景、强调边框和阴影。
- 使用 IntersectionObserver 按视口懒翻译；未进入阅读区域的初始及动态内容不发送给模型。

## v0.1.0 (2026-07-13)

首个可用版本。

### 功能
- 沉浸式双语对照：原文下方直接插入译文。
- 网页整页翻译（右下悬浮「译」按钮 / 右键「翻译本页」），分块并发 + 逐块渲染，首屏秒出。
- 划词翻译气泡。
- **动态内容自动翻译**：翻译后自动监听页面新增节点（菜单 / 面板 / 弹窗），防抖即时翻译。
- **图片翻译**：右键图片或弹窗上传 → 视觉模型 OCR + 译文叠加显示。
- 内置 15 家引擎：OpenAI / Gemini / OpenRouter / DeepSeek / 智谱 / 腾讯混元 / 通义千问 / Kimi / 百川 / 豆包 / Ollama / 自定义 + Google / DeepL / Microsoft 翻译。
- 18 种主流语言互译，可指定源 / 目标语言。
- 本地翻译缓存（内存层 + 防抖回写 + 30 天 TTL + LRU 近似淘汰），省 Token。
- 设置页：测试连接、缓存与术语库开关。
- 苹果风浅色 UI；API Key 仅持久化在本地浏览器，并随请求直发所选服务商。

### 质量
- 新增 MIT LICENSE、.gitignore、GitHub Actions CI（打 zip 并发 Release）。
- fetch 增加 30s 超时与有限重试；4xx 不重试；处理「HTTP 200 但错误体」。
- 图片任务存储读取后即清理，避免 storage.local 堆积。
- 自定义 / 传统引擎可用文本框输入模型名。
- 显式声明内容安全策略。
